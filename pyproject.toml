[project]
name = "chembl-split-check"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "sylphir", email = "mevda0503@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "chembl-webresource-client>=0.10.9",
    "matplotlib>=3.11.1",
    "numpy>=2.5.2",
    "pandas>=3.0.5",
    "rdkit>=2026.3.5",
]

[project.scripts]
chembl-split-check = "chembl_split_check:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
